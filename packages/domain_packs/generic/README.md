# Generic domain pack

Neutral platform defaults only. Safe to commit.

- No API keys, credentials, or real client URLs
- No healthcare / industry-specific regulated content
- No organization names or private data

Clients reference this pack via `domain_pack: generic` and override in `data/clients/<id>/config/`.
