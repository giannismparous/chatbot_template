# Example source whitelist — demo public domains only.

allowed_public_domains:
  - example.com
  - www.example.com

allowed_internal_sources: []

citation_url_rules:
  prefer_public_url: true
  strip_llm_urls: true
  allow_only_whitelisted: true

show_public_sources_only: true
